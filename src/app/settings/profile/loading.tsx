export default function ProfileSettingsLoading() {
  return (
    <main className="auth-shell">
      <section className="auth-card">
        <span className="eyebrow">Profile Settings</span>
        <h1>プロフィールを読み込み中です。</h1>
        <p>数秒待っても進まない場合は再読み込みしてください。</p>
      </section>
    </main>
  );
}
